diff --git a/.loop-engineering/blockers.md b/.loop-engineering/blockers.md
index 03c3487..1368845 100644
--- a/.loop-engineering/blockers.md
+++ b/.loop-engineering/blockers.md
@@ -1,8 +1,3 @@
 # External Blockers
 
-blockers:
-  - id: kujo-cli-module-distribution
-    command: "kujo run <entry>.kujo"
-    evidence: "KUJO_MODULE_PATH now resolves kujo/modules/cli.kujo from external repositories. PackWrite still retains its application-specific parser and needs an explicit adapter contract before replacing it with the smaller first-party parse(spec) API."
-    status: needs-contract-first
-    next_action: "Define the parse(spec) adapter contract for PackWrite's command, validation, and help behavior, then migrate call sites and add parser parity tests."
+blockers: []
diff --git a/.loop-engineering/iterations/001/context.md b/.loop-engineering/iterations/001/context.md
index b9dd8d5..9e743b2 100644
--- a/.loop-engineering/iterations/001/context.md
+++ b/.loop-engineering/iterations/001/context.md
@@ -1,6 +1,9 @@
 # Context
 
 - objective: Replace the pure HLP-001 write_text overwrite helper with native write_file_atomic while preserving staged-pack validation.
-- git_head: 4a90f20
+- git_head: a6ecfe9
 - changed_files:
-  - none
+  - .loop-engineering/blockers.md
+  - .loop-engineering/loop.yml
+  - bin/packwrite
+  - src/cli.kujo
diff --git a/.loop-engineering/loop.yml b/.loop-engineering/loop.yml
index 7bf2734..e4b6f2c 100644
--- a/.loop-engineering/loop.yml
+++ b/.loop-engineering/loop.yml
@@ -17,7 +17,7 @@ blocked_actions:
 
 eval_gates:
   - id: tests
-    command: "KUJO=${KUJO:-../kujo/target/debug/kujo} ./tests/run.sh"
+    command: "KUJO=${KUJO:-../kujo/target/debug/kujo} KUJO_MODULE_PATH=${KUJO_MODULE_PATH:-../kujo/modules} ./tests/run.sh"
     required: true
   - id: diff_check
     command: "git diff --check -- $(git diff --name-only | grep -v '^.loop-engineering/' || true)"
diff --git a/bin/packwrite b/bin/packwrite
index b886045..3f5ea0e 100755
--- a/bin/packwrite
+++ b/bin/packwrite
@@ -25,6 +25,10 @@ done
 PROJECT_DIR="$(cd -P "$(dirname "$SOURCE")/.." >/dev/null 2>&1 && pwd)"
 KUJO="${KUJO:-kujo}"
 
+if [ -z "${KUJO_MODULE_PATH:-}" ] && [ -d "$PROJECT_DIR/../kujo/modules" ]; then
+  export KUJO_MODULE_PATH="$PROJECT_DIR/../kujo/modules"
+fi
+
 if ! command -v "$KUJO" >/dev/null 2>&1 && [ ! -x "$KUJO" ]; then
   echo "packwrite: cannot find the Kujo interpreter ('$KUJO')." >&2
   echo "Set KUJO to your kujo binary, e.g. KUJO=/path/to/kujo/target/release/kujo" >&2
diff --git a/src/cli.kujo b/src/cli.kujo
index 976a22a..58ba0e9 100644
--- a/src/cli.kujo
+++ b/src/cli.kujo
@@ -17,6 +17,7 @@ from src.repo_context import context_collect, context_render
 from src.ai import ai_generate, ai_distillation_prompt, resolve_endpoint, resolve_api_key
 from src.pack import pack_parse_with_meta, pack_validate_manifest, pack_guard_overwrite, pack_apply, pack_validate_config
 from src.validate import validate_run
+from cli import parse_args
 
 VERSION := "0.1.0"
 
@@ -24,45 +25,6 @@ VERSION := "0.1.0"
 # Argument parsing
 # ---------------------------------------------------------------------------
 
-func parse_args(tokens, bool_flags) {
-    mut positionals := []
-    mut flags := {}
-    mut i := 0
-    n := len(tokens)
-    while i < n {
-        tok := tokens[i]
-        if starts_with(tok, "--") {
-            key := substring(tok, 2, len(tok))
-            if in_list(bool_flags, key) {
-                flags[key] := true
-            } else {
-                if i + 1 < n && starts_with(tokens[i + 1], "--") == false {
-                    flags[key] := tokens[i + 1]
-                    i = i + 1
-                } else {
-                    flags[key] := ""
-                }
-            }
-        } else {
-            positionals = push(positionals, tok)
-        }
-        i = i + 1
-    }
-    return {"positionals": positionals, "flags": flags}
-}
-
-func in_list(arr, value) {
-    mut il_i := 0
-    il_n := len(arr)
-    while il_i < il_n {
-        if arr[il_i] == value {
-            return true
-        }
-        il_i = il_i + 1
-    }
-    return false
-}
-
 func first_positional(parsed) {
     p := parsed["positionals"]
     if len(p) >= 1 {
