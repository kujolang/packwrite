diff --git a/packwrite.kujo b/packwrite.kujo
index 9ee736d..25d34a0 100644
--- a/packwrite.kujo
+++ b/packwrite.kujo
@@ -8,7 +8,7 @@
 # or via the bin/packwrite wrapper:
 #   ./bin/packwrite <command> [arguments]
 
-from src.cli import main
+from src.command import main
 
 code := main(args())
 exit(code)
diff --git a/src/cli.kujo b/src/command.kujo
similarity index 94%
rename from src/cli.kujo
rename to src/command.kujo
index 1207c65..35ff915 100644
--- a/src/cli.kujo
+++ b/src/command.kujo
@@ -9,15 +9,14 @@
 #   1  operational failure (missing prompt, AI failure, validation failure)
 #   2  usage error (bad/missing arguments)
 
-from src.util import truthy, is_text
+from src.util import truthy, is_text, list_includes
 from src.errors import is_ok, err_msg
 from src.config import config_load, config_defaults
 from src.prompt import prompt_resolve, prompt_read
 from src.repo_context import context_collect, context_render
 from src.ai import ai_generate, ai_distillation_prompt, resolve_endpoint, resolve_api_key
 from src.pack import pack_parse_with_meta, pack_validate_manifest, pack_guard_overwrite, pack_apply, pack_validate_config
 from src.validate import validate_run
-from cli import parse_args
 
 VERSION := "1.0.0"
 
@@ -36,6 +35,36 @@ func bool_flag_list() {
     return ["dry-run", "overwrite", "verbose", "help", "debug", "strict"]
 }
 
+# Benchmark-only compatibility copy of the parser previously imported from the
+# runtime's source module. Semantics are unchanged.
+func parse_args(tokens, bool_flags) {
+    if type(tokens) != "array" || type(bool_flags) != "array" {
+        return {"positionals": [], "flags": {}}
+    }
+    mut positionals := []
+    mut flags := {}
+    mut ap_i := 0
+    ap_n := len(tokens)
+    while ap_i < ap_n {
+        token := tokens[ap_i]
+        if starts_with(token, "--") {
+            key := substring(token, 2, len(token))
+            if list_includes(bool_flags, key) {
+                flags[key] = true
+            } else if ap_i + 1 < ap_n && starts_with(tokens[ap_i + 1], "--") == false {
+                flags[key] = tokens[ap_i + 1]
+                ap_i = ap_i + 1
+            } else {
+                flags[key] = ""
+            }
+        } else {
+            positionals = push(positionals, token)
+        }
+        ap_i = ap_i + 1
+    }
+    return {"positionals": positionals, "flags": flags}
+}
+
 # ---------------------------------------------------------------------------
 # init
 # ---------------------------------------------------------------------------
